# Files the tree-sitter grammar cannot currently parse, even though the
# Rowan parser (src/syntax/rowan/) accepts them as valid eucalypt.
#
# Catalogued by the eu-wrf7 parser-equivalence audit:
#   docs/superpowers/reports/2026-07-13-parser-equivalence-audit.md
#
# Checked by: python3 scripts/check-tree-sitter-corpus.py
# Re-seed (after deliberately confirming new entries) with:
#   python3 scripts/check-tree-sitter-corpus.py --write-expected
#
# This list should only shrink over time. When a grammar fix resolves an
# entry, delete it here — the check script will flag stale/resolved entries
# as informational but only fails the build on entries NOT in this list, so
# it does not enforce pruning; do it anyway to keep the list honest.
#
# As of eu-2sa6.17's prelude-parsing pass (fix/tree-sitter-prelude-parsing):
# lib/prelude.eu and lib/lens.eu — the original owner-level goal — now parse
# with ZERO error nodes. Verified structurally, not just by exit code (see
# eu-2dxe: a corpus-parse exit code alone cannot rule out a silent
# zero-error-node misparse): tree-sitter's lib/prelude.eu parse has exactly
# 295 top-level `declaration` nodes and 1 top-level `unit_metadata` block,
# matching `eu dump ast lib/prelude.eu`'s 295 top-level DECLARATION nodes
# and 1 top-level BLOCK_META exactly; several individual newly-parsing
# constructs (⌈[x]⌉/⌊[x]⌋ bracket-expr decl heads, the `merge()` doc's
# `s"{..r} → {..s} → {..r, ..s}"` comma-separated row-poly interpolation,
# `ℕ: iota(0)`, and `rejoin([prefix, [s: suffix]])`'s nested list/cons
## parameter pattern) were individually spot-checked against their expected
# shape.
#
# Only 4 entries remain, none of them prelude/lens blockers:

## 023/024 are miscategorised by the audit under "gradual typing" (§2.2) —
## they don't use any gradual-typing syntax at all; they're a BARE
## top-level expression with ZERO declarations (`for.map(negate,
## [1,2,3])` as the entire file). This is a general `source_file` gap (no
## top-level-bare-soup support), not a typing-syntax one. Deliberately NOT
## fixed: the general "unit_metadata = any soup" approach that would cover
## this was tried (in eu-2sa6.17 item 4) and reverted for creating a
## genuine ambiguity with `repeat($.declaration)` that silently misparsed
## ~14 ordinary files with zero error nodes (e.g. `f(x): x` swallowed as a
## 2-element soup instead of a declaration). A safe fix needs the
## soup-vs-declaration ambiguity resolved without that regression risk —
## real GLR-conflict + dynamic-precedence work, not attempted.
tests/harness/typecheck/023_for_map_no_warning.eu
tests/harness/typecheck/024_for_bind_no_warning.eu

## Deliberately-invalid regression fixture: exists specifically to assert
## `eu check --strict` now reports a hard, non-zero exit code for a
## genuine Rowan parse error (eu-3621,
## tests/harness_test.rs's test_typecheck_112_check_strict_parse_error_exits_nonzero).
## Rowan rejects this file by design, so per the audit's own methodology
## (§1: files Rowan itself rejects are excluded, matching
## tests/harness/errors/), this isn't a genuine tree-sitter divergence
## either — kept listed here (rather than moved) only because the
## corpus-parse script's inclusion is purely directory-based
## (tests/harness/, minus tests/harness/errors/) and doesn't know this
## nuance.
tests/harness/typecheck/112_check_strict_parse_error_exits_nonzero.eu

## Chained multi-arg call f(a)(b, c) where the second call has 2+ args.
## Audit report §2.8. Not investigated as part of the prelude-parsing pass
## (doesn't block lib/prelude.eu or lib/lens.eu).
tests/harness/010_prelude.eu
