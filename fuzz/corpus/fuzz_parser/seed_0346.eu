# Type predicate narrows variable in true branch.
# `x` is annotated as `number | string`. In the true branch,
# `x` is narrowed to `number` so `x + 1` produces no warning.
` { type: "(number | string) → number" }
add-one-if-num(x): x number? then(x + 1, 0)

result: add-one-if-num(42)
