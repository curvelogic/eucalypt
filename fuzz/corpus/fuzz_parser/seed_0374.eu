# lookup(:key, Dict(number)) returns number — verified by using the result in a
# numeric context without warning.

` { type: "!Dict(number)" }
scores: { alice: 42, bob: 37 }

` { type: "number → number" }
double(n): n * 2

result: double(lookup(:alice, scores))
