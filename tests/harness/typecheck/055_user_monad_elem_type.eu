# AC3: user-defined typed monad with monad: "[a]". Element type of x
# should be number (from [1, 2, 3]). x * 2 is correct — no warning expected.
` { monad: "[a]" }
my-for: monad({bind(m, f): m mapcat(f), return(v): [v]})

result: { :my-for x: [1, 2, 3] }.(x * 2)
