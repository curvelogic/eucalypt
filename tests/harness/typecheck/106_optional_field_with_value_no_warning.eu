# Optional field syntax: a function typed {name: string, age?: number, ..r} -> string
# called with a record that includes the optional field should produce no warning.
` { type: "{{name: string, age?: number, ..r}} -> string" }
greet(rec): rec.name

result: greet({ name: "Bob", age: 30 })
