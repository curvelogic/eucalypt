# `head` on an empty list literal synthesises `List(never)`,
# which is NOT a subtype of the return annotation `number`.
# The type checker must emit a warning.

` { type: "number" }
bad-head: [] head
