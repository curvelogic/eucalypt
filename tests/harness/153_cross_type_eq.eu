#!/usr/bin/env eu

# Cross-type equality should return false, not error.
# Regression test for PR #720 (native atom returns in arithmetic).

a: 1 = [1]
b: "hello" = [:hello]
c: :foo = 42
d: [] = 0
e: 1 = {x: 1}

RESULT: if(not(a) and(not(b)) and(not(c)) and(not(d)) and(not(e)), :PASS, :FAIL)
