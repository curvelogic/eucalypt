# Exporting a JSON u64 value that overflows i64 to TOML should produce an
# informative error, not an `unreachable!` panic with no context.
` { target: :main format: :toml import: "data=testdata/large_uint.json" }
main: { n: data.n }
