mutation = "hof-bad-arg"
description = "2-arg lambda passed to map, which expects a 1-arg function"
region_start_line = 2
region_end_line = 2
expected_class = "function"
xfail = true
xfail_reason = "eu-1tkk.7.12 does NOT fix this on the DEFAULT (bytecode) engine, verified empirically rather than assumed: with `EU_ERROR_TRACE_DUMP=1` this fixture's env trace is a single `map`-annotated prelude Smid and its stack trace is a single `map`-annotated prelude Smid — there is no user Smid anywhere in either trace, so the Phase 2 classifier has nothing to classify as `User` and curation (which only ever *removes* frames) cannot manufacture one. The curated trace is therefore empty AND the primary is non-user, which is the strict-(iv) violation this xfail records. NOTE the engines diverge here: under `EU_HEAPSYN=1` the error's own Smid IS the user's `result` binding (`error_has_user_file: true`), so the legacy engine satisfies all five invariants on this fixture and the gate's unexpected-pass arm is asserted on the default engine only — see the comment on that assertion in tests/diagnostics_invariants.rs. Closing the gap on the shipping engine is backend annotation-propagation work, not presentation-layer curation — tracked by eu-gvci. Original diagnosis follows. eu-1tkk.7.11: still no user Smid reaches the trace (env/stack trace are entirely prelude frames — 'map's internal recursion), so eu-1tkk.7.8's location-selection fix (never show a prelude primary when a user Smid is available) correctly suppresses the primary to null rather than showing the misleading [prelude]:1463 it used to. eu-1tkk.7.18 (Update-continuation force-site annotation, checked directly against this fixture) does not change this case: the arity-mismatch error is raised from inside map's Case fallback against the *current* annotation at the point the partial application is scrutinised, not by forcing a memoised thunk, so there is no Update continuation on the path back to the user's `xs map(_ + _)` call site to carry it. Getting an actual user primary here needs eu-1tkk.7.11's blame/curation work to surface the user call site into the trace in the first place."
