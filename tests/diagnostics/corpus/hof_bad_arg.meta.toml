mutation = "hof-bad-arg"
description = "2-arg lambda passed to map, which expects a 1-arg function"
region_start_line = 2
region_end_line = 2
expected_class = "function"
xfail = true
xfail_reason = "eu-1tkk.7.11: still no user Smid reaches the trace (env/stack trace are entirely prelude frames — 'map's internal recursion), so eu-1tkk.7.8's location-selection fix (never show a prelude primary when a user Smid is available) correctly suppresses the primary to null rather than showing the misleading [prelude]:1463 it used to. eu-1tkk.7.18 (Update-continuation force-site annotation, checked directly against this fixture) does not change this case: the arity-mismatch error is raised from inside map's Case fallback against the *current* annotation at the point the partial application is scrutinised, not by forcing a memoised thunk, so there is no Update continuation on the path back to the user's `xs map(_ + _)` call site to carry it. Getting an actual user primary here needs eu-1tkk.7.11's blame/curation work to surface the user call site into the trace in the first place."
