mutation = "metadata-span"
description = "runtime type error in a declaration carrying doc metadata"
region_start_line = 3
region_end_line = 3
expected_class = "number"
# Region narrowed from 2..3 to 3..3 (eu-1tkk.7.34). Line 3 is `doubled(xs):
# scale("2", xs)` — the call that passes the offending "2", and so the line the
# user has to edit. Line 2 is `scale`'s own body: true about where the
# multiplication failed, but correct code needing no change. Both lines used to
# satisfy the gate, which left the distinction resting on the golden snapshot
# alone; pinning 3..3 makes the gate itself reject a "fix" that walks the
# primary back onto the callee.
# No xfail: eu-1tkk.7.12 fixed this. The invariant (iv) violation was a
# non-empty trace containing only a 'transparent' [prelude] map frame and no
# user frame. `map` declares `blame: :transparent`, and the Phase 2 curation
# pipeline (`eval::error::curate_trace`) now drops transparent frames
# outright, so the trace is empty while the primary remains the user location
# on line 2 — the "empty trace with a user primary" arm of invariant (iv).
# Live guard from here on: reintroducing an uncurated prelude frame, or losing
# the user primary, fails the gate.
